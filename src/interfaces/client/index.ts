export interface IClientRequest {
    length: number
    id: string
    fullname: string
    email: string
    telefone: string
    registeredAt: Date
    updatedAt: Date
    deletedAt: Date
    userId: string
}